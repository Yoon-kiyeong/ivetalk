CSS를 추가하는 방법
1. 같은 HTML 파일에 HTML코드와 CSS 코드를 놓는 방법
style이라는 태그를 사용해서 CSS를 놓는다
style 태그는 head 태그 안에 있어야 한다
2. CSS와 HTML을 분리시키는 방법
CSS파일은 link를 통해서 연결한다
rel이라는 relationship이라는 attribute를 붙여준다
두 방법 다 상관없지만 그래도 두번째 방법이 제일 유용하다